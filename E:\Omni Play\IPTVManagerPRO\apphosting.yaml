# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure
# For more information on how to build and serve your app, see
# https://firebase.google.com/docs/app-hosting/deploy-nextjs

# The name of the framework used by your app.
# This field is required for all backends.
#
# Must be one of: "nextjs", "angular"
framework: nextjs

runConfig:
  cpu: 1
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
